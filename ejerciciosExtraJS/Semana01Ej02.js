"use strict";

/**
 * Crea una variable llamada `tarjeta` que contenga una `string` con un número de 16 dígitos simulando el número de una tarjeta de crédito
 * (por ejemplo: 1234543278874532), extrae y almacena en una nueva variable llamada
 * `ultimosNumeros` los últimos 4 caracteres de tarjeta.
 * Luego, en la variable `ultimosNumeros` utiliza el método `padStart()` para rellenarla con asteriscos (`*`)
 *  hasta alcanzar una longitud total de 16 caracteres.
 * Por último, imprime en la consola el valor de la variable `tarjeta`, que debería representar un número de tarjeta de crédito con todos los dígitos ocultos,
 *  excepto los últimos cuatro dígitos.
 */
