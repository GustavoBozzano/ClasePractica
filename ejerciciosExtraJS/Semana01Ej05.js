"use strict";

/**
 * Declara una variable llamada `dolares` y asígnale un valor que represente la cantidad de dólares que deseas convertir a euros.
 * Luego, necesitarás la cotización actual del dólar respecto al euro.
 * Declara una constante llamada `tasaCambi` y asigna a esta el valor de la cotización actual
 * (la cantidad de euros que obtienes por un dólar).
 * A continuación, realiza la conversión de dólares a euros.
 * Para hacer esto, multiplica la cantidad de que se encuentra en la variable `dolares` por la `tasaCambio`.
 * Guarda el resultado de este cálculo en una nueva variable llamada `euros`.
 * Por último, para verificar que tu conversión de dólares a euros ha sido realizada correctamente,
 * utiliza la función `console.log()` para imprimir el valor de `euros` en la consola del navegador.
 */
