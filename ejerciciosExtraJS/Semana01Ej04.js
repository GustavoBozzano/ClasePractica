"use strict";

/**
 * Declara una variable `temperaturaFahrenheit` y asigna un valor.
 * Luego, convierte esa temperatura a grados _Celsius_ y guarda el resultado en una variable `temperaturaCelsius`.
 * Imprime el valor de `temperaturaCelsius`.
 * La fórmula para pasar de _Fahrenheit_ a _Celsius_ es la siguiente:
 * `((grados_Fahrenheit − 32) × 5) / 9 = °C`.
 */
