"use strict";

/**
 * Realiza las operaciones siguientes:
 * 1. Elimina el último elemento del array `numeros` y muestra por consola el elemento eliminado.
 * 2. Añade el número 17 al final del array `numeros` y muestra en consola la longitud de dicho array
 * 3. Elimina el primer elemento del array
 * 4. Añade el número 45 al inicio del array
 * 5. Copia los 3 primeros elementos del array `numeros` en un nuevo array llamado `misNumeros`
 * 6. Utiliza el método `splice()` para reemplazar el elemento en la posición 3 del array `numeros` por el número 99
 * 7. Verifica si el array `numeros` contiene el valor 19 y muestra por consola si existe o no en dicho array
 * 8. Invierte el orden de los elementos del array `numeros` y muéstralos por consola.
 */

const numeros = [9, 7, 6, 10, 5];
