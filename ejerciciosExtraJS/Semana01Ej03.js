"use strict";

/**
 * Crea una constante llamada `birthYear` y asigna el año en que nació una persona.
 * Después, crea una variable llamada `currentYear` y asigna el año actual.
 * A continuación, calcula la edad de la persona y resta a `currentYear` el valor de `birthYear` y guarda el resultado en una nueva variable llamada `personAge`.
 * Finalmente, muestra el valor de `personAge` en la consola.
 */
